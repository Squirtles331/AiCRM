# 安全政策（Security Policy）

AiCRM 重视安全问题。如果你发现了安全漏洞，请按照本文件指引报告，我们将优先处理。

## 支持的版本

| 版本 | 支持状态 |
| --- | --- |
| 0.1.x | ✅ 积极支持（维护中） |
| < 0.1.0 | ❌ 不再支持 |

## 报告漏洞

**请不要通过公开的 Issue / PR 报告安全漏洞**，这会将漏洞暴露给所有使用者。

请发送邮件至 **security@aicrm.example.com**，并在邮件中包含：

- 影响版本（`pom.xml` / `pyproject.toml` 中的版本号）
- 漏洞类型与严重程度评估（如：越权访问 / SQL 注入 / 信息泄露 / XSS 等）
- 完整复现步骤（附最小复现代码 / 请求示例更佳）
- 影响范围评估（哪些接口 / 模块受影响）
- 可选的修复建议或补丁

我们建议对敏感细节进行加密或分批发送，避免在邮件正文中一次性暴露完整攻击载荷。

## 处理承诺

| 时间 | 承诺 |
| --- | --- |
| 24 小时内 | 确认收到报告，并给出初步评估 |
| 72 小时内 | 输出修复计划与预计发布时间 |
| 修复后 | 发布补丁版本，并在 [CHANGELOG](./CHANGELOG.md) 中记录（不泄露漏洞细节） |

高危 / 紧急漏洞将优先于普通功能迭代处理，必要时直接发布 hotfix 版本。

## 修复与披露原则

- 修复补丁发布**前**，不对外公开漏洞细节；
- 补丁发布后，在安全通告中披露漏洞信息，并对报告者致谢（如报告者同意）；
- 涉及第三方依赖（如 Spring Boot、PostgreSQL JDBC、Redisson 等）的漏洞，优先同步上游官方修复并升级依赖。

## 安全加固建议（部署方）

- 生产环境通过 `application-prod.yml` 强制关闭 springdoc 文档入口，勿以 `AICRM_DOC_ENABLED=true` 覆盖；
- 通过网关拦截 `/v3/api-docs`、`/swagger-ui*`、`/webjars*` 等路径；
- 外部回调必须校验签名、租户和幂等键；
- 定期轮换 JWT 密钥、外部凭证与系统口令，勿将 `.env` 或凭证提交到代码仓库。
