
# CRM 后端领域化重构与分阶段开发计划

## 1. 总体方案

以现有 CRM 业务文档为产品蓝图，将当前按 `dao/service/api` 横向分层的工程重构为 PostgreSQL 驱动的 DDD 模块化单体。计划按 `pmaster` 的 Must-first 原则组织，每阶段都必须形成可运行、可测试、可独立验收的业务增量。

- 保留 Java 17、Spring Boot、JdbcTemplate、Redis、RabbitMQ、对象存储和内部 `com.aicrm` 命名。
- PostgreSQL 16+ 作为唯一官方数据库，使用 Flyway 管理版本，不兼容 MySQL。
- 仅提供 `/api/v1`；不保留旧接口、旧 DTO、旧 Mapper、旧模块或旧表兼容能力。
- 共享表模式支持多租户；所有租户业务数据必须经过租户、组织、数据范围和字段权限校验。
- 删除模型、问答、预测、推荐、诊断、画像等运行时能力；保留规则、审批、提醒、报表及确定性统计。

## 2. 目标工程架构

建立以下 Maven 领域模块：

- `aicrm-shared-kernel`：统一响应、错误码、值对象、事件协议、分页、幂等与安全上下文。
- `aicrm-platform`：租户、组织、用户、角色、权限、审批、审计、任务、字典、配置和文件。
- `aicrm-sales`：渠道、线索、客户、联系人、跟进、会话、竞争信息、商机和报价。
- `aicrm-catalog`：产品、分类、价格表、价格项和折扣规则。
- `aicrm-trade`：合同、合同变更、销售订单和取消。
- `aicrm-analytics`：工作台查询、报表、目标、指标、计分和结果确认。
- `aicrm-integration`：CRM 受控营销获客 Webhook、组织事件接收回执及后续适配器。
- `aicrm-web`：REST Controller、DTO 转换、OpenAPI 和异常映射。
- `aicrm-admin-boot`：模块装配、基础设施适配、配置和应用启动。

每个领域模块内部固定使用 `api/application/domain/infrastructure` 四层。领域层只能依赖共享内核；跨领域只允许调用公开应用接口或订阅领域事件，禁止导入其他领域的实体、Mapper 和 Repository。使用 ArchUnit 固化依赖规则。

## 3. 数据与接口规范

- 主键使用应用生成的 `BIGINT`，API 中序列化为字符串；业务编号单独使用 `VARCHAR`。
- 聚合根统一包含 `tenant_id`、`status`、`version`、创建/更新人员与时间、`deleted_at`、`deleted_by`。
- 时间统一为 `TIMESTAMPTZ` 并按 UTC 存储；金额使用 `NUMERIC(18,2)`，币种使用 ISO 4217 三字符代码。
- 核心可检索属性必须结构化建列；`JSONB` 只保存扩展字段、规则快照和外部原文。
- 同领域使用物理外键；跨领域只保存租户和聚合标识，由应用服务验证引用。
- 软删除唯一性使用 PostgreSQL 部分唯一索引；所有租户索引以 `tenant_id` 为首要维度。
- 跨领域事件采用事务 Outbox；消费者使用 Inbox 和业务幂等键去重，RabbitMQ 不作为事实数据源。
- Redis仅用于权限缓存、短期锁、限流和热点字典，数据库始终是业务事实源。

首批 API 明确区分私海与公海：

- `GET /api/v1/leads/private`、`GET /api/v1/leads/public`
- `POST /api/v1/leads/{id}/actions/claim|release|assign|transfer|convert|invalidate`
- `GET /api/v1/customers/private`、`GET /api/v1/customers/public`
- `POST /api/v1/customers/{id}/actions/claim|release|assign|transfer|merge`
- 创建、转换、合并、审批提交及连接器配置必须支持 `Idempotency-Key`；连接器入站事件以外部事件编号和载荷哈希幂等。
- DTO、领域对象、持久化对象完全分离；响应统一包含 `code/message/data/traceId`，HTTP 状态码表达真实错误类型。

线索和客户增加 `ownership_type`、`owner_user_id`、`owner_dept_id`、`public_pool_id`、`pool_entered_at`、`last_follow_up_at`、`next_follow_up_at` 和 `version`。认领使用条件更新保证并发下只有一个成功者；回收、转移、合并、转换均记录不可变历史。

## 4. 分阶段交付

### 阶段 0：设计基线

在 `docs/architecture/` 建立领域上下文图、模块依赖、完整数据字典、状态机、API 规范、错误码、事件目录、安全模型和 ADR。完成全部核心表字段、索引、唯一约束、数据主责及状态定义评审后才能进入编码。

### 阶段 1：工程底座与销售主数据

- 1A：完成新模块骨架、Flyway、统一异常、DTO、审计、JWT 服务端权限加载、组织数据范围、测试框架和 Outbox。
- 1B：完成线索私海/公海、客户私海/公海、联系人、跟进、分配、认领、回收、转换、合并和离职交接。
- 旧线索与客户接口、横向模块和建表脚本直接下线；开发数据库按 Flyway 单轨基线重建且不导入旧表数据。
- 验收出口：租户隔离、数据范围、并发认领、定时回收和线索转客户闭环全部通过。

### 阶段 2：产品、商机与报价

完成产品价格表、商机阶段、输单与重启、报价版本、折扣校验、过期和拒绝。引入轻量审批引擎，支持顺序节点、多人任一/会签、条件路由、撤回、转交、驳回和定义快照。

### 阶段 3：合同与销售订单

完成合同签署、变更、作废，订单生成、确认、取消和 CRM 内部关闭。CRM 保存销售合同与销售订单；ERP、财务和售后系统不接入 CRM 交易流程。

### 阶段 4：工作台、绩效、集成与发布

已完成工作台聚合查询、个人目标与结果确认、确定性销售报表、规则驱动目标计分，以及营销 Webhook 获客与组织事件接收回执。下一项为连接器失败重试与运行监控；渠道、会话、文档、话术、竞争信息等能力须按新领域边界重新实现，形成首个稳定开源版本。

## 5. 测试与验收

- JUnit 5 覆盖领域状态机和规则；核心领域行覆盖率不低于 80%，整体不低于 70%。
- Testcontainers 验证 PostgreSQL、Redis、RabbitMQ、Flyway 空库初始化和逐版本升级。
- ArchUnit 保证模块边界，CI 中出现跨领域实体或 Mapper 引用直接失败。
- 安全测试覆盖跨租户、跨部门、私海越权、字段脱敏、审批越权和附件访问。
- 并发测试验证同一公海资源只能被一个用户认领，转换、合并及外部回调重复提交不产生重复数据。
- 契约测试验证 `/api/v1`、错误码、分页和 OpenAPI 定义一致，并断言旧路径不可访问。
- 端到端测试覆盖获客至销售订单，以及无效、回收、合并、输单重启、拒绝、过期、作废、取消和订单关闭。
- 基准环境使用单实例 4C8G、10 个租户和百万级销售数据；常用列表查询 P95 不高于 300ms，业务命令 P95 不高于 500ms。

## 6. 既定假设

- 当前开发数据库可重建，不编写旧开发数据迁移程序。
- 不一次性实现全部业务域；未通过对应阶段门的能力不提供运行时代码或临时表。
- PostgreSQL 是唯一承诺支持的数据库，暂不维护双数据库方言。
- PostgreSQL RLS 作为后续增强，首期以租户拦截器、Repository 约束和隔离测试为强制防线。
- 第一开发里程碑严格限定为工程底座、权限体系及线索/客户私海公海闭环。
