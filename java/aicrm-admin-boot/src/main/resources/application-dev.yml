spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/aicrm
    username: aicrm
    password: aicrm_dev_123
    hikari:
      maximum-pool-size: 20
      minimum-idle: 5
      connection-timeout: 30000
  data:
    redis:
      host: localhost
      port: 6379
      password:
      database: 0
      timeout: 5s
  rabbitmq:
    host: localhost
    port: 5672
    username: aicrm
    password: aicrm_dev_123
    virtual-host: aicrm
    listener:
      simple:
        default-requeue-rejected: false

logging:
  level:
    com.aicrm: debug
